{
  "name": "unmet",
  "private": true,
  "version": "0.1.0",
  "packageManager": "bun@1.4.2",
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "typecheck": "bun run --cwd backend typecheck && bun run --cwd frontend typecheck",
    "test": "bun run test:backend && bun run test:contract",
    "test:contract": "cd backend && forge test -vv",
    "test:backend": "bun run --cwd backend test",
    "build": "bun run typecheck && bun run --cwd frontend build",
    "build:frontend": "bun run --cwd frontend build",
    "check": "bun run typecheck && bun run test:backend && bun run test:contract && bun run build:frontend",
    "dev:backend": "bun run --cwd backend dev",
    "dev:frontend": "bun run --cwd frontend dev"
  }
}
